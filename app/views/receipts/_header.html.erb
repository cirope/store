<header>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title text-center"><%= t 'receipts.header' %></h3>
    </div>
    <div class="panel-body">
      <div class="row">
        <div class="col-md-5">
          <%= render @book.organization_entity %>
        </div>
        <div class="col-md-2">
          <h1 class="text-center"><%= @book %></h1>
        </div>
        <div class="col-md-5 text-right">
          <p class="lead">
            <%= Receipt.model_name.human %> <%= Receipt.human_attribute_name 'number' %>
            <span class="label label-default">
              <%= '%012d' % receipt_number %>
            </span>
          </p>

          <p>
            <strong><%= Receipt.human_attribute_name 'created_at' %></strong>:
            <%= l receipt_date %>
          </p>
        </div>
      </div>
    </div>
  </div>
</header>
