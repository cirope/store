<fieldset class="relation">
  <%= f.input :id, as: :hidden %>
  <%= f.input :organization, as: :autocomplete, label: false, url: organizations_path %>
  <div class="row-controls">
    <%= link_to_remove_nested_item f %>
  </div>
</fieldset>
