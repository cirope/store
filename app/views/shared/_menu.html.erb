<header class="navbar navbar-default navbar-fixed-top" role="banner">
  <div class="container">
    <div class="navbar-header">
      <% if current_user %>
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      <% end %>

      <%= link_to t('app_name'), root_path, class: 'navbar-brand' %>
    </div>

    <% if current_user %>
      <nav class="collapse navbar-collapse" role="navigation">
        <ul class="nav navbar-nav">
          <% if admin_subdomain? %>
            <%= menu_item_for Account, accounts_path %>
          <% else %>
            <li>
              <%= link_to root_path, title: t('launchpad.index.title') do %>
                <span class="glyphicon glyphicon-home"></span>
              <% end %>
            </li>
            <%= render 'shared/submenu/admin' %>
            <%= menu_item_for Customer, customers_path %>
            <%= menu_item_for Item, items_path %>
          <% end %>
        </ul>

        <%= render 'shared/nav_search' %>
        <%= render 'shared/user_actions' %>
      </nav>
    <% end %>
  </div>
</header>
